extends ../layout

block content
    h2#w  ADD HERE
        form(action="/wishs/add",method="post")
            .form-group
                label(for="name") Name
                input#name(type="text",class="form-control", name="name")
            .form-group
                label(for="detail") Details
                input#detail(type="text",class="form-control", name="detail")
            input(type="submit", class="btn btn-info" )

